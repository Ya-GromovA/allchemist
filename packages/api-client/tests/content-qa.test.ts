import type { ContentQaSummary } from "../src";

function assert(condition: unknown, message: string): asserts condition {
  if (!condition) throw new Error(message);
}

export function testContentQaFixtureShape() {
  const fixture: ContentQaSummary = {
    workflowRu: ["????????????????", "Content QA"],
    publishGateRu: "?????????????? ???????????? ?????????????????????? ?????? ????????????????????.",
    requiredMetadata: ["subject", "source_list"],
    statusCounts: { draft: 1 },
    latestBlocks: [],
  };
  assert(fixture.statusCounts.draft === 1, "QA fixture typed");
}
