# ALC-001 Repository Baseline Report

Date: 2026-07-14 (Europe/Moscow)
Scope: non-destructive stabilization of `/root/synapse` and recovery of a restart-safe Next preview artifact.

## Outcome

ALC-001 completed successfully. A protected and verified backup was created, the pre-existing target workspace was attributed, a clean local baseline commit was created in a separate worktree, and a restart-safe preview artifact was built and smoke-tested on `127.0.0.1:3011`.

Production routing, nginx, systemd, Docker Compose, backend, PostgreSQL, and the existing preview on port 3010 were not changed or restarted. No push was performed.

Ready for architecture planning: **YES**.
Ready for preview service switch: **NO**; switching was outside ALC-001 and still requires a separately approved operational task.

## Preflight and comparison with ALC-000

| Check | ALC-001 preflight result |
|---|---|
| Repository root | `/root/synapse` |
| Branch | `figma-full-ui-migration-20260602` |
| HEAD | `d3106a0df8bfe5da2fad87602c13fd9beeddedaa` |
| Modified tracked paths | 34 |
| Untracked paths | 1369 |
| Total status paths | 1403 |
| Existing worktrees | one, `/root/synapse` |
| Port 3010 | PID `1603626`, listening |
| Port 3011 | free |
| ALC-000 comparison | exact branch, HEAD, 34 tracked paths, and 1369 untracked paths matched the recorded ALC-000 final state |

No unexpected post-ALC-000 change was found. The five ALC-000 reports and the previously recorded incomplete `.next` side effect were present as expected.

Storage evidence:

- `/dev/vda1`: 79 GiB total, 54 GiB used, 22 GiB available, 72% utilization.
- `/root/synapse`: 6.9 GiB.
- root `node_modules`: 358 MiB.
- `mobile/node_modules`: 1.9 GiB.
- backend virtual environment: 100 MiB.
- pre-existing `/root/synapse/apps/web/.next`: 188 KiB, with no `BUILD_ID`.

No cleanup was attempted.

## Protected backup

- Path: `/root/backups/allchemist/ALC-001/20260714-112717`
- Directory mode: `0700`
- Regular-file mode: `0600`
- Final size: 22 MiB
- Final regular-file count: 66
- Final checksum manifest: `sha256sum-final.txt`
- Verification result: 60/60 included files passed; 0 failed.

The backup contains Git inventories, worktree and ref inventories without remote URLs, modified tracked source, selected untracked source/config/test/docs/assets, a separately protected runtime-state archive, excluded/quarantine inventories, command logs, build logs, artifact verification, and smoke evidence.

Archive member lists were read after creation. Runtime/user/security files were not copied into Git and their contents were not printed.

## Baseline construction

| Field | Value |
|---|---|
| Source HEAD | `d3106a0df8bfe5da2fad87602c13fd9beeddedaa` |
| Local branch | `baseline/allchemist-recovery-20260714-112717` |
| Worktree | `/root/worktrees/allchemist-recovery-20260714-112717` |
| Local commit | `c18b3d89c38e28b47f46816326981720fb30e253` |
| Commit subject | `chore(baseline): recover pre-ALC-001 target workspace` |
| Commit statistics | 489 files changed, 42,966 insertions, 23,476 deletions |
| Final worktree status | clean |
| Remote branches containing commit | 0 |

The commit was staged from explicit path lists. Neither `git add .` nor `git add -A` was used. Local commit identity parameters were used without changing global Git configuration.

The baseline includes classified source, config, tests, docs, and required assets. It excludes `.next`, `node_modules`, caches, `*.tsbuildinfo`, logs, screenshots generated by tests, databases, runtime/user/security state, secrets, and unknown files.

Fifteen tracked mutable/runtime paths inherited from the source commit were explicitly removed only in the isolated baseline: `assistant_log.md`, backend runtime/security and user-state paths, their `data_host_backup` copies, and mutable APK latest metadata. They remain untouched in `/root/synapse` and are protected in backup where applicable.

`git diff --check` passed after mechanical whitespace normalization of five imported historical Markdown files. The normalized path list is retained in the protected backup.

## Dependency and build evidence

No `preinstall`, `install`, `postinstall`, or `prepare` lifecycle scripts were found in the root, web, admin, mobile, tools, or inspected package manifests.

Executed only in the isolated worktree:

| Command | Exit | Result |
|---|---:|---|
| `npm ci --ignore-scripts --no-audit --no-fund` | 0 | 91 packages installed; lock file unchanged |
| `npm run typecheck:tokens` | 0 | PASS |
| `npm run typecheck:ui` | 0 | PASS |
| `npm run typecheck:ai-assistant` | 0 | PASS |
| `npm run typecheck:web` | 0 | PASS |
| `npm run build:web` | 0 | PASS; Next.js 16.2.10, 11 static pages generated |

Node was `v20.20.2`; npm was `10.8.2`. The root lock hash remained `72c4fcd10592add46c59b8cfed418cb743fb4dee74bd61b2de0c51160eda2326`. The isolated worktree was clean after install and build because generated outputs are excluded.

## Restart-safe artifact

- Release: `/root/allchemist-runtime/preview/releases/20260714-112717`
- Directory mode: `0750`; release files: `0640`
- Size: 60 MiB
- BUILD_ID: `foMZhxf6qk1kQrn7k-Y4I`
- Startup entry: `/root/allchemist-runtime/preview/releases/20260714-112717/apps/web/server.js`
- Manifest: `ARTIFACT_MANIFEST.md`
- Checksum result: 1399/1399 files passed.
- Expected temporary start: `HOSTNAME=127.0.0.1 PORT=3011 node /root/allchemist-runtime/preview/releases/20260714-112717/apps/web/server.js`

The release contains standalone runtime files, `.next/static`, public assets, package metadata, the application BUILD_ID, a root BUILD_ID copy, and SHA-256 checksums. No current-preview symlink or service configuration was changed.

## Runtime preservation

Final production evidence:

- port 3010 still uses PID `1603626`, originally started 2026-07-10 01:22:38 server time;
- its cwd remains `/root/synapse/apps/web/.next/standalone/apps/web (deleted)`;
- `http://127.0.0.1:3010/` remains HTTP 200;
- port 3011 is free after smoke;
- `allchemist.ru/`, `admin.allchemist.ru/`, and `api.allchemist.ru/docs` remain HTTP 200;
- Docker services `synapse-backend` and `synapse-db` remain healthy;
- nginx remains active.

The absent `/health` and `/admin` routes on domains where nginx does not define them are not regressions. Confirmed routing exposes `/api/v1/*`, `/docs`, and `/openapi.json` to the backend.

## Residual risks and do-not-touch boundaries

1. The production checkout remains intentionally dirty and must not be cleaned, reset, or used for builds.
2. The current 3010 process still runs from a deleted cwd and is not restart-safe; the recovered artifact is not active.
3. The baseline commit is local only. Publication requires separate approval and review.
4. Legacy admin JavaScript has the previously recorded duplicate declaration parse failure.
5. Seven of eight approved visual references remain missing, including critical science screens.
6. Backend pytest and database migrations were not run because no isolated database was authorized.
7. Nginx, systemd, Docker Compose, DNS, database state, and production services remain out of scope for changes.

## Recommended next task

`ALC-002 — TARGET ARCHITECTURE PLAN AND MIGRATION GATES`

That task should use commit `c18b3d89c38e28b47f46816326981720fb30e253` as the planning baseline, define ownership and route migration gates, and keep any preview service switch in a separately approved operational task.
