# Allchemist Stage Ledger

Дата фиксации: 2026-05-26

Правило статусов: `СДЕЛАНО И ПРОВЕРЕНО` ставится только после кода, сборки и соответствующих smoke/regression-проверок. Если часть уже есть, но не закрывает требования этапа полностью, статус `ЧАСТИЧНО СДЕЛАНО`.

## Этапы

| Этап | Статус | Фактическое состояние | Следующий критерий перехода |
| --- | --- | --- | --- |
| 1. Stage ledger | СДЕЛАНО И ПРОВЕРЕНО | Ledger создан, сохранён в `/root/synapse/docs/qa/stage-ledger.md` и будет отражён в `assistant_log.md`. Код backend/web/mobile/admin на этом этапе не менялся. | Можно переходить к этапу 2 после фиксации записи в журнале. |
| 2. Роли и вход | СДЕЛАНО И ПРОВЕРЕНО | Public web и mobile onboarding показывают понятные сценарии `Я учусь`, `Я учитель`, `Я родитель`, `У меня есть код доступа`, `Войти по логину и паролю` без свободного выбора служебных ролей. `expectedRole` убран из login/password API contract и клиентов. Backend auth/login, auth/me, invite/preview, invite/activate, role/switch, phone/verify и profile отдают server-driven поля `displayName`, `activeRole`, `availableRoles`, memberships, subscriptions, grants, capabilities, feature flags. Preview школьного кода не расходует активацию и показывает школу/площадку/класс/роль/модули. Web показывает `Режим работы` только для server-assigned roles. Mobile показывает preview школьного кода перед активацией. APK 1.0.6 опубликован как latest. Проверено: backend py_compile, targeted pytest `10 passed`, node `--check`, forbidden-string audit, backend rebuild + health, live invite preview, Playwright visual/auth/admin smoke, mobile TypeScript, Android release build, APK emulator smoke. Login brute-force/device-limit hardening отнесён к этапу 10. | Переход к этапу 3: web layout и роль-зависимые кабинеты. |
| 3. Web layout | СДЕЛАНО И ПРОВЕРЕНО | Desktop public web расширен, authenticated app layout исправлен: sidebar и рабочие секции теперь находятся в единой `workspaceMain` сетке, добавлена верхняя панель кабинета с текущим разделом, ролью и ссылкой на latest APK. После входа заголовок меняется с `Войти в кабинет` на `Личный кабинет`. Роль-зависимые tabs есть для student/teacher/homeroom/parent. Главные role workspaces обновлены: ученик видит `Учебная сводка` со stat/action cards, учитель/классный руководитель видит dashboard классов/задач/проверки/онлайн-уроков и классные списки без видимого userId, родитель видит dashboard детей/уведомлений/рекомендаций без technical details. Из ordinary profile/payment/live UI убраны `Технический ID` и expandable technical JSON details. Закреплён контроль APK latest: `/downloads/apk/latest` теперь в первую очередь отдаёт файл из `allchemist-apk-latest.json`, а тест проверяет metadata/HEAD/download. Authenticated smoke проверяет desktop для всех ролей и mobile viewport для student/teacher/parent, включая видимость workspace title и latest APK link. Проверено: `node --check`, `tests/test_public_web.py` -> `3 passed`, backend rebuild + health, live APK metadata/HEAD, Playwright visual/auth/admin smoke. | Переход к этапу 4: Mobile layout. |
| 4. Mobile layout | СДЕЛАНО И ПРОВЕРЕНО | Mobile bottom tabs оставлены без перегруза: 3 вкладки `Home`, `Analytics`, `Cabinet`. Профиль в `CabinetScreen.tsx` разбит на `Аккаунт`, `Безопасность`, `Офлайн`, `Настройки`, `Помощь`; technical/API/debug wording убран из ordinary mobile UI. Fallback web shell переименован в русский `Алхимик: веб-кабинет`, ссылка `API Docs` скрыта. APK `1.0.7`/`versionCode 8` опубликован как latest: `allchemist-release-20260525-1.0.7-mobile-layout-smoke-verified.apk`, SHA256 `b36ca7885cbc780db11902931af9b379a56a8d98c05e3799f3042f81333cc2ae`, размер `105469335`. Проверено: `npx tsc --noEmit`, ordinary UI audit targeted screens, Android release build, APK emulator smoke, public APK metadata/HEAD, локальная копия APK в `/home/usgromov/Allchemist/apk/`. | Переход к этапу 5: Splash. |
| 5. Splash | СДЕЛАНО И ПРОВЕРЕНО | Исходный `/home/usgromov/Allchemist/apk/доки/zagruzka.png` подтверждён и совпадает с `/root/synapse/mobile/assets/splash/zagruzka.png`, SHA256 `13a7f74d9374e6cbeace7f070b7bb4e0e08a2f9ec5911063180de00bfaba0056`. Splash fullscreen сохраняет весь artwork, progress overlay теперь усиливает встроенный progress-bar без собственной второй подложки/рамки, колба анимируется glow/steam/bubbles/burst. В bootstrap добавлен timeout/error-state для зависшей локальной инициализации. APK `1.0.8`/`versionCode 9` опубликован как latest: `allchemist-release-20260525-1.0.8-splash-smoke-verified.apk`, SHA256 `f1b14bc462552397c4efef2f05551d4443cc75cb7adce26d41559394057cdf72`, размер `105467779`. Проверено: `npx tsc --noEmit`, Android release build, APK emulator smoke со screenshots `/tmp/allchemist-apk-smoke.png`, `/tmp/allchemist-apk-smoke-late-1.png`, `/tmp/allchemist-apk-smoke-late-2.png`, public APK metadata/HEAD, локальная копия APK в `/home/usgromov/Allchemist/apk/`. | Переход к этапу 6: Assets. |
| 6. Assets | СДЕЛАНО И ПРОВЕРЕНО | Подготовленные visuals проверены и подключены без смены текущих обоев. Mobile уже использует `student_dashboard_hero`, `teacher_dashboard_hero`, `parent_dashboard_hero`, `module_chemistry`, `module_physics`, `module_biology`, `module_ai`, `icon_chemistry`, `icon_physics`, `icon_biology`, `icon_ai` в `HomeScreen`. Web public теперь отдаёт и использует `chemistry_lab_hero`, `physics_simulator_hero`, `biology_microscope_hero`, `periodic_table_trainer`, module/dashboard images через `/api/v1/web/assets/...`; учебный module view показывает hero visuals и inline visual cards. Admin web теперь отдаёт те же visuals через `/api/v1/admin/web/assets/...`; showcase, school overview и license panel показывают chemistry/physics/biology/AI/lab/simulator/microscope visuals вместо одной повторяющейся иконки. Проверено: endpoint py_compile, `node --check`, backend rebuild + health, asset GET downloads, `tests/test_public_web.py tests/test_admin_panel.py` -> `5 passed`, mobile `npx tsc --noEmit`, Playwright visual/auth/admin smokes. Mobile assets не менялись, поэтому новый APK не выпускался; latest остаётся `1.0.8`. | Переход к этапу 7: Учебные экраны web/mobile. |
| 7. Учебные экраны web/mobile | СДЕЛАНО И ПРОВЕРЕНО | Web module view теперь показывает явный учебный маршрут `теория → практика → лаборатория/симулятор/микроскоп → тест → AI`, отдельные блоки теории, практики, лабораторного/симуляторного шага, тестов/экзаменов и AI-разбора для chemistry/physics/biology. Mobile Biology больше не MVP-preview: экран показывает последовательный маршрут, визуальный блок `Виртуальный микроскоп`, переход в AI-разбор по biology и аналитику biology. APK `1.0.9`/`versionCode 10` опубликован как latest: `allchemist-release-20260525-1.0.9-stage7-learning-smoke-verified.apk`, SHA256 `079f7ecd181aa85f5ad9714f941413fd552cd542f3def3993a046d391fe9d79b`, размер `105469655`. Проверено: `node --check`, `tests/test_public_web.py` -> `3 passed`, backend rebuild + health, Playwright visual/auth/admin smokes, mobile `npx tsc --noEmit`, Android release build, APK emulator smoke, public APK metadata/HEAD, локальная копия APK. Полноценные научные 3D/симуляторы и расширенный corpus остаются задачей контентного производства/этапа 9. | Переход к этапу 8: Admin. |
| 8. Admin | СДЕЛАНО И ПРОВЕРЕНО | Admin UX закрыт для текущего stage scope: главная показывает `Границы полномочий`, раздел школ содержит создание школы/классов/кодов и `Чеклист перед выдачей лицензии`, content-раздел показывает `Редакционный маршрут без автопубликации` с source/draft/review/publish gate, help содержит документы и `Сценарии со скриншотами`. Backend role checks усилены: `school_admin` сохраняет доступ к admin panel, но не может создавать глобальные школы и менять global rights overrides; эти операции требуют `admin` или `owner`. Проверено: py_compile admin endpoint/tests, `node --check` admin JS, `tests/test_admin_panel.py` -> `4 passed`, backend rebuild + health, Playwright admin authenticated smoke, visual smoke, live served markers. | Переход к этапу 9: Контентный pipeline. |
| 9. Контентный pipeline | СДЕЛАНО И ПРОВЕРЕНО | Content QA pipeline закрыт для текущего stage scope: есть source intake с license metadata, duplicate guard для источников и блоков, `content_hash`-дедупликация, очереди workflow `/api/v1/content/qa/queues`, статусы `draft → author_review → scientific_review → methodist_review → content_qa → legal_review → published`, no-autopublish gate и проверка существующих source IDs перед публикацией. Admin content UI показывает `Очереди workflow и дедупликация`, source/block registry, history, publish gate и редакционный маршрут. Проверено: py_compile content/admin tests, `node --check` admin JS, `tests/test_content_platform_catalog.py tests/test_admin_panel.py` -> `8 passed`, focused content regression -> `4 passed`, backend rebuild + health, Playwright admin authenticated smoke, visual smoke, live served markers. Реальный массовый verified corpus и научная production-проверка материалов остаются контентной работой, а не кодовым блокером этого этапа. | Переход к этапу 10: Security/subscriptions. |
| 10. Security/subscriptions | СДЕЛАНО И ПРОВЕРЕНО | Security/subscriptions закрыт для текущего stage scope: password login получил rate-limit/lockout и audit, device registration теперь привязывает deviceId к текущей session и revoke устройства отзывает связанные session, device recovery activation rate-limited и audited, добавлены self-service `/api/v1/users/export` и `/api/v1/users/delete` с подтверждением `DELETE`, удаление отзывает sessions и удаляет login/phone/consent/entitlements/device/access state с сохранением redacted tombstone. Tenant/global checks из этапа 8 сохранены: `school_admin` не может создавать глобальные школы и менять global rights. Проверено: py_compile, `tests/test_auth_sync_contract.py tests/test_admin_panel.py` -> `10 passed`, backend rebuild + health, Playwright visual/auth/admin smokes, mobile `npx tsc --noEmit`. | Переход к этапу 11: Production infrastructure. |
| 11. Production infrastructure | СДЕЛАНО И ПРОВЕРЕНО | Production infrastructure закрыт как code/ops baseline без разрушительных prod-изменений: добавлен `/root/synapse/docs/production-readiness-gate-ru.md` с release gates, monitoring baseline, backup/restore evidence, payments/legal blockers и manual blockers; добавлен исполняемый `/root/synapse/tools/stage11-production-preflight.sh`. Проверено: preflight прошёл (`APK metadata file is consistent`, production health endpoint, public APK metadata endpoint, Docker health `synapse-backend`), public health `200`, public APK metadata `1.0.9`/`versionCode 10`/SHA256 `079f7ecd181aa85f5ad9714f941413fd552cd542f3def3993a046d391fe9d79b`, APK HEAD `Content-Length: 105469655`, backend regression `tests/test_public_web.py tests/test_admin_panel.py tests/test_auth_sync_contract.py tests/test_content_platform_catalog.py` -> `17 passed`, mobile `npx tsc --noEmit`, Playwright visual/admin/auth smokes. Первый authenticated roles smoke при параллельном запуске дал transient miss `Учебная сводка`; targeted body dump подтвердил текст на странице, повторный smoke прошёл. Не заявлено как решённое: Object Storage/CDN, CI/CD promotion/secrets, external monitoring/alerts, off-server restore drill evidence, payment provider E2E и финальная legal validation. | Переход к этапу 12: Full smoke. |
| 12. Full smoke | СДЕЛАНО И ПРОВЕРЕНО | Финальный full smoke после этапов 1–11 выполнен. Найден и закрыт один regression blocker: `test_content_quality_snapshot.py` падал из-за устаревшего formula/symbol snapshot после уже внесённых Stage 7–11 изменений; snapshot обновлён штатно через `python3 tools/content_quality_guard.py --update-snapshot`, targeted test -> `2 passed`. Проверено: backend py_compile для ключевых endpoint/service файлов, полный `pytest` backend -> `36 passed`, `node --check app/web_public/app.js app/web_admin/app.js`, backend rebuild `docker compose up -d --build synapse-backend`, health `200 {"status":"ok","service":"allchemist-api"}`, Playwright visual/authenticated/admin smokes, mobile `npx tsc --noEmit`, production preflight, public APK metadata/HEAD (`1.0.9`, `versionCode 10`, SHA256 `079f7ecd181aa85f5ad9714f941413fd552cd542f3def3993a046d391fe9d79b`, `Content-Length: 105469655`). Mobile code/assets не менялись, поэтому новый APK не выпускался; latest остаётся Stage 7 artifact `1.0.9`. Не заявлено как решённое: реальные внешние blockers Stage 11 и полноценный контентный corpus/scientific production review. | Этапы 1–12 закрыты в текущем code/ops scope; следующий шаг только по отдельному product/ops решению. |
| 13. Интерактивная таблица элементов | СДЕЛАНО И ПРОВЕРЕНО | После закрытия этапов 1–12 начат backlog по незакрытым acceptance criteria. Web chemistry module получил интерактивную таблицу 118 элементов: атомный номер, символ, название, цвет по семейству, режимы `Изучение`, `Свойства`, `Тренировка`, `Запоминание`, sticky-карточка выбранного элемента со свойствами, применением/где встречается, осторожностью, мнемоникой и source/verified metadata. Mobile `PeriodicTableScreen.tsx` обновлён: режимы, подсказка `Для полного вида поверните телефон`, расширенная карточка, источники и безопасность. Исправлен `tools/apk-emulator-smoke.sh`, чтобы проверка `dumpsys window` не падала ложно из-за `pipefail`/`grep -q`. APK `1.0.10`/`versionCode 11` опубликован как latest: `allchemist-release-20260527-1.0.10-periodic-table-smoke-verified.apk`, SHA256 `021bc0b147a651a1706962bbec2b1e706c6919a6769063b1c5bf40432ad2ebe3`, размер `105472911`. Проверено: `node --check`, `tests/test_public_web.py` -> `4 passed`, targeted Playwright periodic table smoke -> 118 cells/Fe/memory mode, backend rebuild + health, visual smoke, mobile `npx tsc --noEmit`, Android release build, APK emulator smoke, public APK metadata/HEAD, local APK copy SHA256/size, production preflight, authenticated roles smoke, `tests/test_public_web.py tests/test_content_quality_snapshot.py` -> `6 passed` после штатного snapshot update. Остаток: часть элементов в mobile пока использует справочный fallback по массе `см. источник`, а не индивидуальную массу в карточке для всех 118; полноценный научный corpus и расширенные element facts остаются content-production задачей. | Следующий backlog: углубить учебные модули химии/физики/биологии или закрывать внешний production ops. |
| 14. Массы элементов в mobile | СДЕЛАНО И ПРОВЕРЕНО | Закрыт остаток этапа 13 по mobile-карточкам: в `/root/synapse/mobile/app/screens/PeriodicTableScreen.tsx` добавлен `ATOMIC_MASS_BY_SYMBOL` для всех 118 элементов, и карточка теперь берёт массу из enriched details, затем из полного fallback-справочника, затем только при отсутствии символа показывает `см. источник`. Android release поднят до `versionCode 12`, `versionName 1.0.11`. APK `1.0.11` опубликован как latest: `allchemist-release-20260527-1.0.11-periodic-mass-smoke-verified.apk`, SHA256 `f1812e569bc20f9dc771e50d726055b8d67aaf9f6e96b83b4ac3793e7ee7f7bf`, размер `105472239`. Проверено: mobile `npx tsc --noEmit`, Android release build, APK emulator smoke, public APK metadata/HEAD (`200`, `Content-Length: 105472239`, APK attachment filename), local APK copy, production preflight, backend targeted regression `tests/test_content_quality_snapshot.py tests/test_public_web.py` -> `6 passed`. Физическая ARM-проверка устройства и автоматические app updates остаются внешними/операционными ограничениями. | Следующий backlog: углубить учебные модули химии/физики/биологии или закрывать внешний production ops. |
| 15. Production hardening gate | СДЕЛАНО И ПРОВЕРЕНО | Добавлен проверяемый hardening gate `/root/synapse/tools/stage15-production-hardening-check.sh`, документ `/root/synapse/docs/ops/stage15-production-hardening-ru.md`, исправлен `/root/synapse/tools/backup_synapse.sh`: корректный путь `/root/synapse/backend/data/user_state.json`, `SYNAPSE_STATE_PATH` override, SHA256 manifest, права `700` для backup dir и `600` для artifacts. По подтверждению владельца выполнена controlled rotation: перед изменением создан backup `/root/synapse/backups/stage15-secret-rotation-20260527-174404`, сгенерированы новые `POSTGRES_PASSWORD`/`JWT_SECRET` без вывода значений в лог, обновлён пароль роли PostgreSQL `synapse`, синхронно обновлены `infra/.env` и `backend/.env`, backend перезапущен. Важно: rotation инвалидирует старые access JWT по подписи, но не отзывает refresh-сессии; web-клиент может автоматически получить новый access token и остаться в кабинете. Проверено: `bash -n`, public/local health `200`, Stage 15 gate -> `OK: stage15 production hardening check completed`, default secret markers не найдены, backup/restore evidence проходит, backup permissions `700/600`, targeted backend regression `tests/test_content_quality_snapshot.py tests/test_public_web.py tests/test_auth_sync_contract.py` -> `12 passed`. | Следующий backlog: CI/CD в реальном репозитории, external monitoring/alerts, CDN/Object Storage, payment provider E2E, legal sign-off, physical Android ARM smoke/release-key backup; отдельно решить policy принудительного global logout для техокон. |
| 16. Forced logout для техокон | СДЕЛАНО И ПРОВЕРЕНО | Добавлен явный механизм принудительного отзыва refresh/access-сессий: `/root/synapse/backend/app/services/user_state_store.py` получил `revoke_all_sessions`, state получил `session_revocations`, отозванные sessions получают `revokedAt`, `revokedBy`, `revokeReason`, событие пишется в `auth_audit`. Добавлен CLI `/root/synapse/tools/revoke_sessions.py`: dry-run по умолчанию, фактический отзыв только через `--apply`, есть optional `--user-id`, `--changed-by`, `--reason`, автозапуск через backend venv. Документировано в `/root/synapse/docs/ops/stage16-forced-logout-ru.md`. На production `--apply` не запускался, активные пользователи не выбрасывались. Проверено: py_compile service/tool, focused forced logout + role-switch tests -> `2 passed`, CLI dry-run работает, backend regression `tests/test_auth_sync_contract.py tests/test_public_web.py tests/test_content_quality_snapshot.py` -> `13 passed`, backend rebuild, local/public health `200`, Stage 15 gate -> `OK: stage15 production hardening check completed`. | Следующий backlog: CI/CD в реальном репозитории, external monitoring/alerts, CDN/Object Storage, payment provider E2E, legal sign-off или учебный content production. |
| 17. CI/CD baseline | СДЕЛАНО И ПРОВЕРЕНО | Добавлен `.github/workflows/ci.yml` с backend PostgreSQL regression, backend py_compile, public/admin `node --check`, ops tools syntax и mobile `npx tsc --noEmit`. Добавлен `.github/workflows/production-release-gate.yml` как manual release gate с `apk_version`, проверкой `allchemist-apk-latest.json`, наличия APK artifact и release regression. Документировано в `/root/synapse/docs/ops/stage17-cicd-baseline-ru.md`. Production deploy по SSH не добавлен намеренно без repository secrets/protected environments. Проверено: workflow structure check, web/ops syntax, backend regression `tests/test_auth_sync_contract.py tests/test_public_web.py tests/test_content_quality_snapshot.py` -> `13 passed`, mobile `npx tsc --noEmit`, Stage 15 gate -> `OK: stage15 production hardening check completed` и теперь видит CI/CD workflow directory. | Следующий backlog: подключить workflow в реальном VCS/provider с protected environments/secrets, external monitoring/alerts, CDN/Object Storage, payment provider E2E, legal sign-off или учебный content production. |
| 18. Real VCS provider connection | ЧАСТИЧНО СДЕЛАНО | Реальное подключение GitHub/GitLab проверено и заблокировано внешним доступом: `/root/synapse` не git repository, git remote отсутствует, `gh`/`glab` не установлены/не авторизованы, provider token/repository owner access не передан. Добавлен `/root/synapse/tools/github_provider_setup.py`: dry-run по умолчанию, с `--apply` и `GITHUB_TOKEN`/`GITHUB_REPOSITORY` создаёт GitHub environments `dev/stage/prod`, branch protection и repository secrets через `gh secret set`. Документировано в `/root/synapse/docs/ops/stage18-provider-connection-ru.md`. Проверено: `python3 -m py_compile`, dry-run для `owner/repo` печатает GitHub API actions и список missing secrets. | Следующий критерий: предоставить реальный git repository/remote, GitHub/GitLab admin token, `gh`/`glab` auth или выполнить script на машине с provider-доступом; только после этого можно ставить `СДЕЛАНО И ПРОВЕРЕНО`. |
| 19. Monitoring probe baseline | СДЕЛАНО И ПРОВЕРЕНО | Добавлен `/root/synapse/tools/production_monitor_probe.py`: проверяет public health, latency, APK latest metadata, обязательные metadata поля, HEAD latest APK и совпадение `Content-Length` с `sizeBytes`; поддерживает JSON и Prometheus text output. Документировано в `/root/synapse/docs/ops/stage19-monitoring-baseline-ru.md`. Проверено: py_compile, JSON probe `ok=true` для `https://api.allchemist.ru/api/v1`, Prometheus probe `allchemist_monitor_ok 1`, APK versionCode `12`, size `105472239`, health/APK checks OK. Реальный внешний monitoring provider всё ещё требует аккаунт/секреты владельца. | Следующий backlog: подключить probe к внешнему monitoring provider/cron/systemd, CDN/Object Storage, payment provider E2E, legal sign-off или учебный content production. |
| 20. CDN/Object Storage readiness | СДЕЛАНО И ПРОВЕРЕНО | Подготовлен backend к CDN/Object Storage без переключения production на непроверенный provider: `/api/v1/content/downloads/apk/latest/metadata` поддерживает `APK_CDN_BASE_URL`; без env `downloadUrl` остаётся локальным, с env `downloadUrl`/`cdnDownloadUrl` указывают на `<APK_CDN_BASE_URL>/<apkFile>`, `localDownloadUrl` всегда остаётся fallback. Добавлен `/root/synapse/tools/cdn_object_storage_readiness.py` для проверки metadata/artifact/size и формирования CDN URL. Документировано в `/root/synapse/docs/ops/stage20-cdn-object-storage-readiness-ru.md`. Проверено: py_compile endpoint/tool, `tests/test_public_web.py tests/test_content_quality_snapshot.py` -> `7 passed`, readiness tool с примером CDN URL OK, web syntax OK, backend rebuild, local/public health `200`, monitoring probe OK, Stage 15 gate OK. Реальный bucket/CDN hostname/TLS/cache/upload policy всё ещё требует provider credentials. | Следующий backlog: выбрать CDN/Object Storage provider и задать `APK_CDN_BASE_URL`, payment provider E2E, legal sign-off или учебный content production. |

## План ближайшего блока

Следующий этап: отдельное product/ops решение по внешним provider blockers или учебному content production.

Порядок дальнейших работ:

1. Не менять архитектуру/auth/roles/payments/user data без отдельного решения.
2. Для публичного запуска отдельно закрыть внешние blockers: CDN/Object Storage, CI/CD, monitoring/alerts, restore drill evidence, payment E2E, legal validation.
3. Для учебного качества отдельно выполнить content production: verified corpus, научная/методическая проверка, реальные симуляторы/3D assets.
4. Перед любым новым релизом повторять Stage 11 preflight и Stage 12 smoke по изменённому scope.

## Текущий запрет на изменения

Этапы 1–17 и 19–20 закрыты в текущем code/ops scope. Этап 18 частично сделан и заблокирован отсутствием реального git provider-доступа. Внешние provider blockers не заявлять закрытыми без фактических провайдерских секретов/evidence.
